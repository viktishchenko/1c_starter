**Q06 разбор задач t12-t13**

- проверять реквизиты Справочников на Р→Свойства→Использование(для элемента/для группы/&то&др) иначе программный обход может завершиться неочивидной ошибкой(!)
- ДополнительныеСвойства()

    <details>
    <summary>пример</summary>

  ```
  проверить новый элемент или нет, по пустой ссылке ПередЗаписью(), добавив результат в ДополнительныеСвойства() и ПриЗаписи() использовать свойство:
  ```

  ```js
  // Модуль Справочник.Контрагенты
  Процедура ПередЗапсью(Отказ)
  ЭтоНовый = Ссылка.Пустая();
  ДополнительныеСвойства.Вставить("ЭтоНовый",ЭтоНовый);
  КонецПроцедуры

  Процедура ПриЗаписи(Отказ)
  Если ДополнительныеСвойства.ЭтоНовый Тогда
  //создать договор
  НовыйДоговор = Справочники.ДоговорыКонтрагентов.СоздатьЭлемент();
  НовыйДоговор.Владелец = Ссылка;
  НовыйДоговор.Номер = "1";
  НовыйДоговор.ДатаДоговора = ТекущаяДата();
  НовыйДоговор.Наименование = "Основной договор";

  Если Покупатель Тогда
    НовыйДоговор.ВидДоговора = Перечисления.ВидыДоговоров.СПокупателем;
  ИначеЕсли Поставщик Тогда
    НовыйДоговор.ВидДоговора = Перечисления.ВидыДоговоров.СПоставщиком;
  ИначеЕсли ПрочиеОтношения Тогда
    НовыйДоговор.ВидДоговора = Перечисления.ВидыДоговоров.Прочее;
  КонецЕсли

  НовыйДоговор.Записать();

  КонецПроцедуры

  ```

    </details>

- ЗаписьИсторииДанных()

    <details>
    <summary>изменение данных - ретроспектива</summary>

  ```
  Свойства→Справочник→ИсторияДанных(исп/не исп)
  ```

    </details>

- ОбменДанными()

  <details>
  <summary>пример</summary>

  ```
  Если ОбменДанными.Загрузка Тогда
    Возврат;
  КонецЕсли;
  ```

  </details>

- директивы компиляции (&НаКлиенте,&НаСервере,&НаСервереБезКонтекста,&НаКлиентеНаСервереБезКонтекста)

- картинки(прог)

  <details>
  <summary>пример</summary>

  ```js
  &НаКлиенте
    Процедура УстановитьКартинку(Команда)
      Элементы.ПоказатьОстатки.Картинка = БиблиотекаКартинок.НазваниеКартинки;
    КонецПроцедуры
  ```

  </details>

- оптимизация запросов (выз.сервера, контекст.форм)

  <details>
  <summary>пример</summary>

  ![form][def_form]

  ```js
  &НаКлиенте
    Процедура ПриАктивизацииСтроки(Элемент)

      ТекКонтрагент = Элементы.Список.ТекущаяСтрока;
      Контакты = ПолучитьКонтрагенты(ТекущаяСтрока);

      ЮрАдрес = Контакты.ЮрАдрес;
      ФактАдрес = Контакты.ФактАдрес;
      Телефон = Контакты.Телефон;
      ЭлПочта = Контакты.ЭлПочта;

    КонецПроцедуры

  &НаСервереБезКонтекста
    Функция ПолучитьКонтрагенты(Контрагент)

      КонтрагентОбъект = Корнтагент.ПолучитьОбъект();

      СтруктураДанных = Новый Структура("ЮрАдрес","ФактАдрес","Телефон","ЭлПочта");

      Для каждого СтрокаКИ Из КонтрагентОбъект.КонтактнаяИнформация Цикл
        Если СтрокаКИ = Справочники.ВидыКонтактнойИнформации.ЮридическийАдресКонтагента Тогда
          СтруктураДанных.ЮрАдрес = СтрокаКИ.Значение;
        ИначеЕсли СтрокаКИ = Справочники.ВидыКонтактнойИнформации.ФактическийАдресКонтагента Тогда
          СтруктураДанных.ФактАдрес = СтрокаКИ.Значение;
        ИначеЕсли СтрокаКИ = Справочники.ВидыКонтактнойИнформации.ТелефонКонтагента Тогда
          СтруктураДанных.Телефон = СтрокаКИ.Значение;
        ИначеЕсли СтрокаКИ = Справочники.ВидыКонтактнойИнформации.ЭлектроннаяПочтаКонтагента Тогда
          СтруктураДанных.ЭлПочта = СтрокаКИ.Значение;
        КонецЕсли;

        Возврат СтруктураДанных;

    КонецФункции
  ```

  </details>

[def_form]: img/q06-formOpt-form.png

- контроль дублей справочника

    <details>
    <summary>пример</summary>

  ```js
  //модуль объекта (простой вариант проверки для текущего справочника)
  Процедура ПередЗаписью(Отказ)

    НайденныйКонтрагент = Справочники.Контрагенты.НайтиПоНаименованию("Наименование", Истина);

      Если НайденныйКонтрагент.Пустая() = Ложь И НайденныйКонтрагент <> Ссылка Тогда
        Отказ = Истина;
        Сообщение = Новый СообщениеПользователю;
        Сообщение.Текст = СтрШаблон("Контрагент с таки наименованием уже есть в справочнике: %1 (%2)",
                                    НайденныйКонтрагент.Наименование,
                                    НайденныйКонтрагент.Код);
        Сообщение.Сообщить();
      КонецЕсли;

  КонецПроцедуры

  ```

  ```js
  //общий модуль (простая, универсальная проверка для текущего справочника)
  Процедура КонтрольДублей(ТекОбъект,Отказ) Экспорт

  Наименование = ТкеОбъект.Наименование;
  Ссылка = ТкеОбъект.Ссылка;

  НайденныйКонтрагент = Справочники.Контрагенты.НайтиПоНаименованию("Наименование", Истина);

    Если НайденныйКонтрагент.Пустая() = Ложь И НайденныйКонтрагент <> Ссылка Тогда
      Отказ = Истина;
      Сообщение = Новый СообщениеПользователю;
      Сообщение.Текст = СтрШаблон("Контрагент с таки наименованием уже есть в справочнике: %1 (%2)",
                                  НайденныйКонтрагент.Наименование,
                                  НайденныйКонтрагент.Код);
      Сообщение.Сообщить();
    КонецЕсли;

  КонецПроцедуры

  //модуль справочника для проверки дублей
  Процедура ПередЗаписью(Отказ)

    //какой-то код
    //
    //

    РаботаСоСправочникамиСервер.КонтрольДублей(ЭтотОбъект, Отказ);

  КонецПроцедуры

  ```

  ```js
  //общий модуль (простая проверка для разных справочников)
  Процедура КонтрольДублей(ИмяСправочника,ТекОбъект,Отказ) Экспорт

    //защита от дурака
  Если Метаданные.Справочники.Найти(ИмяСправочника) = Неопределено Тогда
      Возврат;
  КонецЕсли;

  Наименование = ТкеОбъект.Наименование;
  Ссылка = ТкеОбъект.Ссылка;

  НайденныйКонтрагент = Справочники[ИмяСправочника].НайтиПоНаименованию("Наименование", Истина);

    Если НайденныйКонтрагент.Пустая() = Ложь И НайденныйКонтрагент <> Ссылка Тогда
      Отказ = Истина;
      Сообщение = Новый СообщениеПользователю;
      Сообщение.Текст = СтрШаблон("%1: элемент с таким наименованием уже есть в справочнике: %2 (%3)",
                                  ИмяСправочника,
                                  НайденныйКонтрагент.Наименование,
                                  НайденныйКонтрагент.Код);
      Сообщение.Сообщить();
    КонецЕсли;

  КонецПроцедуры

  //модуль справочника для проверки дублей
  Процедура ПередЗаписью(Отказ)

  //какой-то код
  //
  //

  МетаданныеТекСправочника = Метаданные();
  ИмяСправочника = МетаданныеТекСправочника.Имя;

  РаботаСоСправочникамиСервер.КонтрольДублей(ИмяСправочника, ЭтотОбъект, Отказ);

  КонецПроцедуры

  ```

  ![check_double][def_double]

  ```js
  //общий модуль (УНИВЕРСАЛЬНАЯ проверка для разных справочников С ПОДПИСКОЙ НА СОБЫТИЯ)

  //Подписки на события → КонтрольДублейВСправочнике
  Процедура КонтрольДублейВСправочнике(Источник, Отказ) Экспорт

  МетаданныеТекСправочника = Источник.Метаданные();
  ИмяСправочника = МетаданныеТекСправочника.Имя;
  РаботаСоСправочникамиСервер.КонтрольДублей(ИмяСправочника, ЭтотОбъект, Отказ);

  КонецПроцедуры

  Процедура КонтрольДублей(ИмяСправочника,ТекОбъект,Отказ) Экспорт

    //защита от дурака
  Если Метаданные.Справочники.Найти(ИмяСправочника) = Неопределено Тогда
      Возврат;
  КонецЕсли;

  Наименование = ТкеОбъект.Наименование;
  Ссылка = ТкеОбъект.Ссылка;

  НайденныйКонтрагент = Справочники[ИмяСправочника].НайтиПоНаименованию("Наименование", Истина);

    Если НайденныйКонтрагент.Пустая() = Ложь И НайденныйКонтрагент <> Ссылка Тогда
      Отказ = Истина;
      Сообщение = Новый СообщениеПользователю;
      Сообщение.Текст = СтрШаблон("%1: элемент с таким наименованием уже есть в справочнике: %2 (3%)",
                                  ИмяСправочника,
                                  НайденныйКонтрагент.Наименование,
                                  НайденныйКонтрагент.Код);
      Сообщение.Сообщить();
    КонецЕсли;

  КонецПроцедуры

  ```

    </details>

[def_double]: img/q06-podpiskaDobley.png

- количество договоров

    <details>
    <summary>пример</summary>

  ```js

  Процедура КоличествоДоговоров()
  //найти контрагента
  КонтрагентСсылка = Справочники.Контрагенты.НайтиПоРеквизиту("ИНН","196874587");
    Если КонтрагентСсылка.Пустая() Тогда
      Сообщение = Новый СообщениеПользователю;
      Сообщение.Текст = "Контрагента с ИНН 196874587 нет в справочнике";
      Сообщение.Сообщить();
      Возврат;
    КонецЕсли;
  //получить все договоры контрагента
    Выборка = Справочники.ДоговорыКонтрагентов.Выбрать(,КонтрагентСсылка);
    Количество = 0;

    Пока Выборка.Следующий() Цикл
      Количество = Количество + 1;
    КонецЦикла;

    Сообщение = Новый СообщениеПользователю;
    Сообщение.Текст = СтрШаблон("Найдено %1 договоров", 1%, Количество);
    Сообщение.Сообщить();

  КонецПроцедуры

  ```

    </details>
