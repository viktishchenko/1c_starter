**Т13**

- клиент-серверная архитектура
  - клиент(интерактив на клинте)
  - сервер(все вычисления на сервере)
  - субд
- програмная работа с элементами и реквизитами(значениями) управляемой формы
  - **элементы** формы(отображение)
  - **реквизиты** формы(данные)
- директивы компиляции
  - &НаКлиенте
  - &НаСервере(форма с данными передается на сервер!!!)

    ```
    Пример: УчебнаяКонфигурация/Обработки/УправляемаяФорма/Формы/Модуль/Процедура Директива_НаСервереБезКонтекста()
    или в статьеhttps://infostart.ru/1c/articles/682305/
    ```

  - &НаСервереБезКонтекста(но ведь данные можно получить и на сервере!!!)
  - &НаСервереНаСервереБезКонтекста

- механизмы взаимодействия
  - команды(кнопка на форме)
  - события(возникновение события формы или элемента)
    ```
    флаг сохранения изменяемых данных в форме !!! (при использовании команд(реквизитов/объекта формы) для изменения реквизитов)
    ```
- практические задачки

  ```
  динамическое изменение формы справочника,
  заполнение наименования по кнопке,
  заполнение наименования по событию,
  поиск и измениние текущей инф. об элементе формы
  ```

  ```
  ОповещениеОбИзменении как способ обновить данные формы на клиенте
  ```

  ```
  отображение необходимой инф. в форме
  ```

  ```
  проверить НОВЫЙ ЭЛЕМЕНТ СПРАВОЧНИКА ИЛИ НЕТ (проверка на пустоту ссылки → Объект.Ссылка.Пустая())!!!
  ```

  ```
  получение инф. из регистра сведений и отображение в выбранном месте (текущий курс валют)
  ```

  ```
  получение данных API, диалоговое отображение заполнение на форме, табличной части, проверка
  ```
