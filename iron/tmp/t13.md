**Т13**

- клиент-серверная архитектура
  - клиент(интерактив на клинте)
  - сервер(все вычисления на сервере)
  - субд
- програмная работа с элементами и реквизитами(значениями) управляемой формы
  - **элементы** формы(отображение)
  - **реквизиты** формы(данные)
- директивы компиляции
  - &НаКлиенте
  - &НаСервере(форма с данными передается на сервер!!!)
    <!-- Пример: УчебнаяКонфигурация/Обработки/УправляемаяФорма/Формы/Модуль/Процедура Директива_НаСервереБезКонтекста() -->
    <!-- или в статьеhttps://infostart.ru/1c/articles/682305/ -->
  - &НаСервереБезКонтекста(но ведь данные можно получить и на сервере!!!)
  - &НаСервереНаСервереБезКонтекста
- механизмы взаимодействия
  - команды(кнопка на форме)
  - события(возникновение события формы или элемента)
