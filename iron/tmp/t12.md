**Т12**

- пользовательский интерфейс: обычный, командный, такси;
- переключение п.и.;
- ком. интерфейс, подсистемы;
- форма списка, элемента, выбора/выбора групп(для иерархического справочника) и т.д.;
- простая форма, управляемая форма;
- различие м/у реквизит формы(не сохраняется) и реквизит объекта(сохраняется);
